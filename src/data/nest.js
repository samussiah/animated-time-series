import measure from './nest/measure';
import timepoint from './nest/timepoint';
import id from './nest/id';

export default function nest() {
    //const nested = 
    //    d3.group()
    //    .key((d) => d.measure)
    //    .entries(this.data);

    return nested;
}
